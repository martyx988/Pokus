## T18: M1 Provenance Ledger and Policy Exception Record

### Purpose
Close M1 process traceability gaps by creating an auditable task-to-branch-to-commit ledger and explicitly recording the M1 direct-on-main exception.

### Scope
Build a single M1 provenance record that maps each existing M1 task to its implementation commit SHA, branch provenance (when available), and merge path; record direct-on-main violations as explicit exceptions with rationale.

### Allowed Changes
- `task_phase/reviews/M1_workflow_review.md`
- `task_phase/reviews/M1_gaps.md`
- One new provenance artifact under `task_phase/reviews/` for M1 mapping evidence

### Forbidden Changes
- Do not rewrite git history.
- Do not modify production code or tests.
- Do not change milestone scope or acceptance criteria.

### Inputs
- Existing M1 task files (`task_phase/tasks/M1/T1.md` ... `T17.md`)
- Repository git history and refs available in current clone
- Current M1 review findings

### Relevant Context
The workflow requires one-task-per-branch provenance and no direct implementation commits on `main`. M1 review found these controls violated or not durably auditable from current refs. Since history rewrite is out of scope, closure requires durable evidence plus explicit exception recording.

### Implementation Budget
- Production files: 0.
- Behavior count: 1 behavior (process evidence capture and exception recording).
- Model/API/workflow count: 0 model, 0 API, 1 process-workflow documentation update.
- Test scope: verify every M1 task has ledger coverage and each exception entry references concrete commit SHA(s).

### Expected Output
A durable M1 provenance ledger exists and review docs reference it, including explicit exception entries for direct-on-main implementation commits.

### Acceptance Criteria
- Every M1 task (T1-T17) appears in the provenance ledger with commit SHA evidence.
- Branch provenance is captured where auditable; unavailable branch provenance is explicitly marked.
- Direct-on-main M1 implementation commits are listed as policy exceptions with concrete SHAs.
- `M1_workflow_review.md` and `M1_gaps.md` are updated to reference the ledger and exception record.

### Test Requirements
Cross-check ledger rows against task files and `git log` so no M1 task is missing and all referenced SHAs resolve.

### Dependencies
T17.

### Stop Condition
Stop when the provenance ledger and exception record are complete, linked from M1 review docs, and no unresolved provenance blind spots remain unmarked.

### Status
Done.

### Completion Summary
- Added durable provenance ledger at `task_phase/reviews/M1_provenance_ledger.md` covering T1-T17 with commit evidence, branch provenance (or explicit unavailable), and merge path.
- Recorded explicit M1 direct-on-main policy exceptions with concrete SHAs in the provenance ledger.
- Updated M1 review artifacts to reference the provenance ledger and exception record.
