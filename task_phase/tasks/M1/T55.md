## T55: Repair M1 Alembic Revision Chain Consistency

### Purpose
Fix the concrete migration-lineage break that blocks Milestone 1 integration-gate startup.

### Scope
Align Alembic revision identifiers and `down_revision` references for Milestone 1 and adjacent migrations so the migration graph resolves deterministically.

### Allowed Changes
- `project/migrations/versions/*.py` files directly involved in the broken revision chain
- `task_phase/tasks/M1/T55.md` status updates on completion

### Forbidden Changes
- Do not add new domain fields or behaviors.
- Do not modify Milestone 2+ feature logic.
- Do not redesign migration history beyond minimal lineage repair.

### Inputs
- `task_phase/reviews/M1_workflow_review.md` failed-check evidence
- Existing migration files including `0010_provider_exchange_reliability_score_schema.py` and `0011_instrument_outcome_classification.py`

### Relevant Context
M1 is blocked because integration tests cannot start when migration lineage is unresolved. This is a structural reliability defect, not a scope expansion.

### Implementation Budget
- Production files: 1-3 migration files in the same lineage segment.
- Behavior count: 1 behavior (valid migration graph resolution).
- Model/API/workflow count: 0 new models, 0 APIs, 1 migration-startup workflow.
- Test scope: migration graph/load startup only.

### Expected Output
A coherent Alembic revision chain where all referenced revision IDs exist and resolve.

### Acceptance Criteria
- All migrations in the affected chain resolve without missing revision errors.
- No schema-scope expansion is introduced.
- Evidence command output is recorded in completion summary.

### Test Requirements
- Run a migration-resolution command (for example Alembic history/current/upgrade path check) proving chain integrity.

### Dependencies
- T17

### Stop Condition
Stop when migration lineage errors are eliminated and startup dependency resolution succeeds.

### Completion Format
Implementation agent must finish task file with:
- `### Status`
- `Done.`
- `### Completion Summary`
- concise summary of completed work

### Status
Done.

### Completion Summary
- Corrected `0011_instrument_outcome_classification` lineage metadata to reference `0010_provider_exchange_reliability_score_schema` consistently (`Revises` and `down_revision`).
- Verified migration graph resolution with `alembic -c project/alembic.ini history`; chain resolves without missing revision errors.
- Verified resulting head state with `alembic -c project/alembic.ini heads`; affected chain resolves to a single head.
