## T20: Live PostgreSQL M1 Integration Evidence or Explicit Waiver

### Purpose
Resolve the remaining M1 product gate blocker by either capturing live PostgreSQL integration-gate execution evidence or recording an explicit policy waiver.

### Scope
Execute the existing M1 integration gate against a reachable PostgreSQL instance with schema-create privileges and capture command/result evidence; if environment access is unavailable, document a formal waiver decision in M1 review artifacts.

### Allowed Changes
- `task_phase/reviews/M1_workflow_review.md`
- `task_phase/reviews/M1_gaps.md`
- `task_phase/tasks/M1/T17.md` (status/result addendum only)
- One evidence artifact under `task_phase/reviews/` for command output summary

### Forbidden Changes
- Do not add new production features or test cases.
- Do not alter milestone scope definitions.
- Do not mark `Open Questions: none` unless live evidence exists or waiver is explicitly recorded.

### Inputs
- Existing integration gate test: `Project/tests/test_m1_integration_gate.py`
- Existing focused validation command documented in T17
- Runtime environment variables (`TEST_DATABASE_URL` or `DATABASE_URL`)

### Relevant Context
M1 product/workflow review is blocked because live PostgreSQL integration evidence was not captured in the current run. Milestone closure requires either successful live gate evidence or explicit policy waiver documentation.

### Implementation Budget
- Production files: 0.
- Behavior count: 1 behavior (evidence capture and gate-state documentation).
- Model/API/workflow count: 0 model, 0 API, 1 validation-gate workflow update.
- Test scope: execute only the pre-existing M1 integration gate path and record pass/fail/skip with environment conditions.

### Expected Output
M1 review artifacts include definitive live-gate evidence or explicit waiver, and open-question status is updated accordingly.

### Acceptance Criteria
- A dated evidence record exists for live PostgreSQL gate execution result, or a dated explicit waiver decision exists.
- `M1_workflow_review.md` reflects updated Product Gate and Open Questions status.
- `M1_gaps.md` removes or marks the live-gate blocker as resolved via evidence/waiver path.
- T17 status addendum references the final evidence or waiver artifact.

### Test Requirements
Run the documented focused Milestone 1 validation command with PostgreSQL connectivity when available; otherwise validate waiver record completeness and linkage.

### Dependencies
T17.

### Stop Condition
Stop when one closure path is complete (live evidence or explicit waiver), documented, and linked in M1 review artifacts.

### Status
Done.

### Completion Summary
- Executed the documented focused Milestone 1 validation command on 2026-05-01; result `OK (skipped=1)` with live PostgreSQL gate skipped because `TEST_DATABASE_URL`/`DATABASE_URL` were unset.
- Added explicit waiver evidence at `task_phase/reviews/M1_T20_live_gate_waiver_2026-05-01.md`.
- Updated M1 review/gap artifacts and T17 addendum links to reflect closure via waiver path.
