## T50: Refresh Read Models After Publication

### Purpose
Refresh publication-dependent read models for readiness and current-day opening prices after publication updates.

### Scope
Implement read-model refresh trigger/execution path tied to publication updates only.

### Allowed Changes
- Read-model builder/refresh modules for readiness and current-day price views
- Publication-to-read-model refresh trigger wiring
- Focused tests for refresh correctness and stale-view safety behavior

### Forbidden Changes
- Do not add new publication decision behavior.
- Do not implement endpoint handlers.
- Do not implement operator table query surface.

### Inputs
- T49 publication status records
- Existing read-model infrastructure from prior milestones
- `design_phase/architecture.md` read-model refresh expectations

### Relevant Context
App and operator reads should come from precomputed trusted views, refreshed only after publication workflow updates.

### Implementation Budget
- Production files: max 2-4 read-model refresh files.
- Behavior count: 1 behavior (refresh readiness/current-day read models after publication changes).
- Model/API/workflow count: 0 new models, 0 APIs, 1 refresh workflow.
- Test scope: refresh includes only published-ready current-day data and preserves fail-closed behavior.

### Expected Output
Read-model refresh path produces current readiness and current-day publication views from publication records.

### Acceptance Criteria
- Read models are refreshed when publication records change for opening-load flows.
- Ready current-day values are represented for app usage; non-ready remain unavailable.
- Refresh path is deterministic and safe under reruns.

### Test Requirements
- Tests for read-model content after ready and blocked publication states.
- Tests for rerun/idempotent refresh behavior.

### Dependencies
- T49

### Stop Condition
Stop when publication-driven read-model refresh for readiness/current-day data is working and tested.

### Completion Format
Implementation agent must finish task file with:
- `### Status`
- `Done.`
- `### Completion Summary`
- concise summary of completed work

