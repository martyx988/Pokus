## T46: Maintain Exchange-Day Opening Load Aggregate State

### Purpose
Compute and persist exchange-day opening-load aggregate counters and lifecycle status from instrument outcomes.

### Scope
Implement aggregate state update logic only for opening-load exchange/day units (eligible count, success/failure counts, start/finish timing, in-progress transitions).

### Allowed Changes
- Exchange-day load aggregate service/model update paths
- Worker workflow hooks that update aggregate state from instrument outcomes
- Focused tests for aggregate counter integrity and state transitions

### Forbidden Changes
- Do not compute publication readiness decisions yet.
- Do not run correctness benchmark validation.
- Do not expose read-model/API outputs.

### Inputs
- T45 instrument outcome records
- Milestone 1/2 exchange-day load schema and supported-universe denominator source
- `design_phase/architecture.md` exchange-day load and publication workflow intent

### Relevant Context
Publication logic needs a reliable exchange/day aggregate state first. This task provides the operational totals and lifecycle states used by later coverage and readiness checks.

### Implementation Budget
- Production files: max 2-4 exchange-day load aggregate files.
- Behavior count: 1 behavior (maintain exchange-day aggregate load state).
- Model/API/workflow count: 1 aggregate model family, 0 APIs, 1 workflow state updater.
- Test scope: aggregate counters, state transition ordering, start/finish timing persistence.

### Expected Output
Exchange-day opening-load records reflect accurate aggregate lifecycle and counts derived from instrument outcomes.

### Acceptance Criteria
- Eligible, success, and failure counters are maintained from instrument outcome changes.
- Exchange-day aggregate status transitions correctly across not-started/in-progress/terminal progression.
- Aggregate timing fields (start/finish) are populated consistently for completed runs.

### Test Requirements
- Tests for counter consistency under incremental outcome updates.
- Tests for transition behavior under mixed success/failure runs.

### Dependencies
- T45

### Stop Condition
Stop when aggregate load state is correctly maintained and tested, without publication decisions.

### Completion Format
Implementation agent must finish task file with:
- `### Status`
- `Done.`
- `### Completion Summary`
- concise summary of completed work

