## T52: Expose Public Current-Day Opening Price Endpoint

### Purpose
Implement public current-day opening-price API read behavior that serves data only for ready exchange/day publications.

### Scope
Add current-day price endpoint handler/serialization and fail-closed access behavior for non-ready states.

### Allowed Changes
- Public API current-day price handler/serializer/routing
- Read-model query layer for current-day published opening prices
- Focused endpoint tests for ready vs non-ready visibility

### Forbidden Changes
- Do not add readiness endpoint changes beyond required wiring reuse.
- Do not alter publication decision rules.
- Do not add operator/private dashboard behavior.

### Inputs
- T50 current-day publication read model
- T51 readiness endpoint patterns
- `design_phase/product_spec.md` FR-26, FR-31a, FR-31b

### Relevant Context
Current-day opening prices are app-visible only when exchange/day publication is ready; otherwise data must remain unavailable.

### Implementation Budget
- Production files: max 2-4 API/read handler files.
- Behavior count: 1 behavior (serve ready-only current-day opening prices).
- Model/API/workflow count: 0 models, 1 API surface group, 0 workflow changes.
- Test scope: fail-closed visibility rules for not-ready/blocked/failed states.

### Expected Output
Public current-day price endpoint returns only published-ready opening-price data and blocks non-ready data exposure.

### Acceptance Criteria
- Ready exchange/day returns current-day opening-price payload from read model.
- Non-ready, blocked, failed, or market-closed states do not expose current-day opening prices.
- Endpoint behavior is consistent with readiness rules.

### Test Requirements
- API tests for ready and non-ready cases.
- Tests for blocked-validation and market-closed non-visibility paths.

### Dependencies
- T51

### Stop Condition
Stop when current-day price endpoint is implemented and tested with fail-closed behavior.

### Completion Format
Implementation agent must finish task file with:
- `### Status`
- `Done.`
- `### Completion Summary`
- concise summary of completed work

### Status
Done.

### Completion Summary
- Added a public current-day opening-price API route at `/app/exchanges/{exchange}/prices/current`.
- Backed the route with a read-model query helper that only returns prices for ready exchange/day publications and fail-closes for blocked, failed, and market-closed states.
- Added focused endpoint coverage for ready and non-ready cases, plus retained readiness endpoint regression coverage.
