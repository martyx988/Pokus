## T47: Compute Coverage and Terminal-Gate Eligibility

### Purpose
Implement exchange/day publication pre-gate checks for terminal-outcome completion and coverage greater than 99%.

### Scope
Add eligibility computation logic only: all-eligible terminal check and coverage percentage against the eligible denominator.

### Allowed Changes
- Publication precheck service/helper logic
- Supporting aggregate/denominator query helpers
- Focused tests for coverage and terminal gating rules

### Forbidden Changes
- Do not implement correctness benchmark mismatch evaluation.
- Do not assign final publication status (`ready`, `failed`, etc.).
- Do not add API endpoints.

### Inputs
- T46 exchange-day aggregate state
- Milestone 2 eligible supported-universe denominator behavior
- `design_phase/product_spec.md` FR-28, FR-30, NFR-4, AC-11

### Relevant Context
The trust model requires terminal outcomes for eligible instruments and greater than 99% coverage before an exchange/day can proceed toward `ready` publication.

### Implementation Budget
- Production files: max 2-3 publication precheck files.
- Behavior count: 1 behavior (compute terminal+coverage publication eligibility).
- Model/API/workflow count: 0 new models, 0 APIs, 1 publication precheck behavior.
- Test scope: denominator handling, >99% threshold, and incomplete-terminal blocking.

### Expected Output
Publication precheck outputs explicit pass/fail eligibility flags for terminal completeness and coverage.

### Acceptance Criteria
- Precheck fails when any eligible instrument lacks terminal outcome.
- Coverage is computed against eligible instrument denominator and requires greater than 99%.
- Precheck output is deterministic and reusable by downstream publication decision logic.

### Test Requirements
- Tests for 100%, >99%, and <=99% coverage outcomes.
- Tests for non-terminal instrument cases blocking eligibility.

### Dependencies
- T46

### Stop Condition
Stop when terminal+coverage eligibility checks are implemented and tested.

### Completion Format
Implementation agent must finish task file with:
- `### Status`
- `Done.`
- `### Completion Summary`
- concise summary of completed work

