## T54: Milestone 4 Integration Gate and Trust-Loop Validation

### Purpose
Validate Milestone 4 end-to-end trust-loop wiring from scheduled opening loads through publication gating, app visibility rules, and operator visibility.

### Scope
Add focused M4 integration tests and commands. Allow only tiny defect fixes required to satisfy gate assertions.

### Allowed Changes
- Milestone-focused integration tests under `project/tests/`
- Minimal fixtures for launch-exchange opening publication scenarios
- `task_phase/tasks/M4/T54.md` status updates on completion
- Tiny defect fixes directly required by failing M4 gate assertions

### Forbidden Changes
- Do not add Milestone 5 historical-close features.
- Do not add new domain scope outside Milestone 4 acceptance criteria.
- Do not introduce broad dashboard features beyond M4 operator table assertions.

### Inputs
- Outputs of T43-T53
- Milestone 4 acceptance criteria from `task_phase/roadmap_plan.md`
- `design_phase/product_spec.md` FR-26, FR-28, FR-30, FR-31a, FR-31b, FR-43, FR-55c, AC-11, AC-25

### Relevant Context
Milestone 4 is complete only when the app sees ready-only current-day data and operators can inspect blocked/failed reasons with required load-quality evidence.

### Implementation Budget
- Production files: 0-1 tiny defect-fix files only if needed by gate failures.
- Behavior count: 1 behavior (integration validation of M4 trust loop).
- Model/API/workflow count: 0 new models, 2 API surface groups validated (readiness/current-day), 1 integrated publication workflow.
- Test scope: full M4 chain only.

### Expected Output
Runnable M4 integration gate proving publication fail-closed behavior, >99% coverage + correctness requirements, and required operator-table observability.

### Acceptance Criteria
- Integration tests cover scheduled opening-load execution, per-instrument outcomes, exchange/day aggregate state, terminal+coverage gating, correctness-validation blocking, publication status decisions, read-model refresh, readiness API, current-day API, and today-opening operator table.
- Tests assert current-day opening prices are hidden until `ready`, and remain hidden for blocked/failed/non-ready states.
- Tests assert market-closed handling skips price computation and produces expected exchange/day status.
- Tests include repeated timeliness-miss scenario that marks internal degraded state without exposing partial-quality app data.

### Test Requirements
- Execute focused M4 integration gate command(s).
- Record pass/fail/skip conditions and environment assumptions in completion summary.

### Dependencies
- T43
- T44
- T45
- T46
- T47
- T48
- T49
- T50
- T51
- T52
- T53

### Stop Condition
Stop when M4 integration evidence exists and no Milestone 4 acceptance gap remains unaddressed.

### Completion Format
Implementation agent must finish task file with:
- `### Status`
- `Done.`
- `### Completion Summary`
- concise summary of completed work

### Status
Done.

### Completion Summary
- Added `Project/tests/test_m4_integration_gate.py` to cover the Milestone 4 trust loop end to end: scheduler smoke, ready vs blocked vs market-closed publication states, app readiness/current-price visibility, operator today-opening visibility, and a repeated timeliness-miss validation failure.
- Focused validation run: `PYTHONPATH=src python -m unittest tests.test_m4_integration_gate tests.test_app_exchange_readiness_endpoints tests.test_operator_opening_load_table tests.test_opening_read_model_refresh tests.test_opening_load_outcome_classification -v` from `Project/` passed.
- Assumptions: SQLite-backed isolated fixtures only; no external services were required; the scheduler call is treated as execution proof while load rows are seeded directly for deterministic gate coverage.
