## T61: Implement Concrete M4 Runtime Trust-Loop Execution Path

### Purpose
Provide concrete runtime execution evidence for Milestone 4 from opening-load scheduling through publication/read-model flow against the real worker/database boundary.

### Scope
Wire and document a reproducible runtime path (non-fixture seeded) that executes scheduler/worker load processing and publication refresh using real persistence flow.

### Allowed Changes
- `project/src/**` worker orchestration/runtime command surfaces and minimal runtime wiring needed for concrete execution
- `project/tests/**` only for focused non-fixture integration/smoke coverage of runtime boundary
- Runtime documentation for execution command(s)
- `task_phase/tasks/M4/T61.md` status updates on completion

### Forbidden Changes
- Do not add Milestone 5 historical-close features.
- Do not satisfy evidence solely by direct seeded publication rows.
- Do not broaden into unrelated dashboard or governance work.

### Inputs
- `task_phase/reviews/M4_workflow_review.md` concrete-evidence blockers
- `design_phase/product_spec.md` FR-26 through FR-33, FR-43, FR-55b through FR-55d

### Relevant Context
M4 must demonstrate the real trust loop at runtime, not only in-process helper assertions over seeded fixtures.

### Implementation Budget
- Production files: 2-5 closely related worker/runtime files.
- Behavior count: 1 behavior (concrete runtime execution of trust loop boundary).
- Model/API/workflow count: 0 new models, up to 1 runtime command surface, 1 integrated worker publication workflow.
- Test scope: runtime boundary execution and publication/read-model evidence.

### Expected Output
A concrete non-fixture runtime command/path proving worker-driven opening-load -> publication decision -> read-model refresh flow.

### Acceptance Criteria
- Runtime execution path runs through real DB-backed workflow states, not direct seeded terminal rows.
- Evidence includes worker execution boundary, publication state transition, and read-model/API-visible effect.
- Configuration and run instructions are reproducible.

### Test Requirements
- Execute at least one non-fixture runtime/smoke command plus focused assertions for publication/read-model result.

### Dependencies
- T54

### Stop Condition
Stop when concrete runtime trust-loop evidence is captured without fixture-only shortcuts.

### Completion Format
Implementation agent must finish task file with:
- `### Status`
- `Done.`
- `### Completion Summary`
- concise summary of completed work
