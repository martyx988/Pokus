## T48: Run Correctness Benchmark Validation for Opening Publication

### Purpose
Evaluate exchange-day correctness using fixed benchmark sample and 5% mismatch threshold, producing a publication-blocking result when validation is delayed or failed.

### Scope
Implement correctness validation execution/result persistence only.

### Allowed Changes
- Validation module behavior for benchmark sample evaluation in publication flow
- Publication quality result persistence fields/helpers
- Focused tests for mismatch threshold and delayed/failed validation blocking output

### Forbidden Changes
- Do not finalize publication status transitions.
- Do not implement app/operator endpoint behavior.
- Do not alter source-prioritization rules from Milestone 3.

### Inputs
- T47 terminal+coverage precheck output
- Milestone 3 validation evidence foundation and benchmark basis
- `design_phase/product_spec.md` FR-31a, FR-31b, AC-25, NFR-8

### Relevant Context
Current-day opening publication must stay blocked until correctness validation succeeds. Delayed or failed benchmark checks are serious quality failures.

### Implementation Budget
- Production files: max 2-4 validation/publication-quality files.
- Behavior count: 1 behavior (compute and persist correctness-validation publication block/pass result).
- Model/API/workflow count: 0-1 quality result model touchpoints, 0 APIs, 1 validation workflow.
- Test scope: 5% mismatch threshold and delay/failure blocking semantics.

### Expected Output
Exchange-day correctness-validation result is persisted with explicit pass/fail/blocked context for publication decisions.

### Acceptance Criteria
- Benchmark mismatch logic uses fixed sample semantics and 5% threshold.
- Validation delay or failure yields an explicit publication-blocking result.
- Successful validation yields explicit pass result consumable by publication decision step.

### Test Requirements
- Threshold tests at, below, and above 5% mismatch boundary.
- Tests for delayed/failure validation producing blocked result.

### Dependencies
- T47

### Stop Condition
Stop when correctness-validation results are produced and stored for publication gating.

### Completion Format
Implementation agent must finish task file with:
- `### Status`
- `Done.`
- `### Completion Summary`
- concise summary of completed work

