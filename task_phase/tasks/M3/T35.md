## T35: Global Source-Prioritization Selection Engine

### Purpose
Implement deterministic source selection for conflicting candidate values using the global policy order.

### Scope
Build selection engine logic and decision-evidence output only.

### Allowed Changes
- Source-prioritization service modules
- Selection decision-evidence data structures tied to selection output
- Focused unit tests for policy precedence and deterministic ties

### Forbidden Changes
- Do not implement provider adapter calls.
- Do not implement validation report workflows.
- Do not expose API endpoints.

### Inputs
- T34 candidate-value persistence output
- `design_phase/product_spec.md` FR-18, FR-19, FR-19c, AC-24
- Milestone 3 policy order from `task_phase/roadmap_plan.md`

### Relevant Context
Milestone 3 requires one global policy: provider/exchange reliability score, then historical availability ratio, then exchange coverage quality, then fixed source order. Selection must be explainable.

### Implementation Budget
- Production files: max 2-4 prioritization-focused files.
- Behavior count: 1 behavior (deterministic candidate selection).
- Model/API/workflow count: 0-1 evidence model touch, 0 APIs, 1 selection behavior.
- Test scope: policy-layer precedence, tie-break determinism, and evidence payload completeness.

### Expected Output
Selection engine returns one winning candidate value with reproducible ordering evidence.

### Acceptance Criteria
- Selection precedence follows the exact global policy order.
- Output includes evidence fields showing evaluated ranking inputs and winner reason.
- Tie scenarios resolve deterministically by fixed source order.

### Test Requirements
- Unit tests for each precedence layer and deterministic tie behavior.

### Dependencies
- T34

### Stop Condition
Stop when selection logic is deterministic, evidence-bearing, and fully unit-tested.

### Completion Format
Implementation agent must finish task file with:
- `### Status`
- `Done.`
- `### Completion Summary`
- concise summary of completed work


### Status
Done.

### Completion Summary
- Implemented a deterministic global source-prioritization engine that ranks candidates by reliability score, historical availability ratio, exchange coverage quality, and fixed source order.
- Added selection evidence output including policy order, ranked candidate inputs, winning candidate key, and explicit winner reason for explainability.
- Added focused unit tests covering each precedence layer, fixed-order tie resolution, deterministic full-tie behavior, and evidence completeness.
