## T36: Provider/Exchange Reliability Score Model and Update Routine

### Purpose
Compute and persist provider/exchange reliability scores from validation-style outcomes.

### Scope
Implement reliability score storage and update routine only. No full validation report assembly in this task.

### Allowed Changes
- Reliability score model/service modules
- Focused model/migration updates for provider/exchange score fields
- Score update routine from outcome inputs
- Focused unit tests for scoring update behavior

### Forbidden Changes
- Do not change source-prioritization order logic.
- Do not implement exchange-level validation verdict reporting.
- Do not add operator dashboards.

### Inputs
- T33 provider attempt outcomes
- T35 selection engine score-consumer requirements
- `design_phase/product_spec.md` FR-19a, FR-19b

### Relevant Context
Milestone 3 requires provider reliability maintained per provider and exchange, with updates based on validation/production-style outcomes (benchmark matches, missing rate, timeliness, stale data, provider errors, disagreement frequency).

### Implementation Budget
- Production files: max 2-4 reliability-focused files.
- Behavior count: 1 behavior (reliability score update and persistence).
- Model/API/workflow count: 1 model path, 0 APIs, 1 scoring behavior.
- Test scope: score update math/order and repeat-update consistency.

### Expected Output
Provider/exchange reliability scores can be updated and persisted from outcome metrics and consumed by source-prioritization.

### Acceptance Criteria
- Score updates are scoped per provider and exchange.
- Update routine ingests required outcome dimensions and persists deterministic score changes.
- Repeated updates for same input window produce stable outcomes.

### Test Requirements
- Unit tests for score-update computation and persistence consistency.
- Consumer compatibility test proving prioritization can read updated scores.

### Dependencies
- T35

### Stop Condition
Stop when reliability scoring is persisted and test-verified without broader reporting workflow additions.

### Completion Format
Implementation agent must finish task file with:
- `### Status`
- `Done.`
- `### Completion Summary`
- concise summary of completed work

