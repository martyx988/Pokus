## T37: Launch Exchange Validation Run Orchestrator and Report Skeleton

### Purpose
Create the validation-run workflow entrypoint and report skeleton for launch exchanges (NYSE, Nasdaq, PSE).

### Scope
Implement validation job/command orchestration shell and persisted report skeleton fields only, without detailed metric collectors.

### Allowed Changes
- Worker/admin validation command or job entry modules
- Validation run/report model/service skeleton modules
- Focused workflow tests for run lifecycle and persisted report shell

### Forbidden Changes
- Do not implement full metric calculations in this task.
- Do not alter publication readiness logic.
- Do not introduce new app-facing endpoints.

### Inputs
- Milestone 3 validation goal from `task_phase/roadmap_plan.md`
- `design_phase/product_spec.md` FR-56, FR-56a, FR-56b, AC-26, AC-27
- T22 launch exchange baseline records

### Relevant Context
Milestone 3 must produce explicit validation outcomes for launch exchanges, including clear pass/fail status and actionable findings, especially for PSE trust-bar blocking.

### Implementation Budget
- Production files: max 2-4 validation-run focused files.
- Behavior count: 1 behavior (start/persist validation run and report shell).
- Model/API/workflow count: 1 validation-run model path, 0 APIs, 1 worker/admin workflow.
- Test scope: lifecycle state transitions and report record creation/update.

### Expected Output
System can start a launch exchange validation run and persist a structured report shell for later metric population.

### Acceptance Criteria
- Validation entrypoint accepts target exchange(s) and creates a run record.
- Report skeleton persists per-exchange result buckets and final verdict fields.
- Workflow supports rerun without duplicate-state corruption.

### Test Requirements
- Workflow tests for create/start/finish or fail transitions of validation runs.
- Persistence tests for per-exchange report shell records.

### Dependencies
- T36

### Stop Condition
Stop when validation run orchestration and report shell persistence are implemented and tested.

### Completion Format
Implementation agent must finish task file with:
- `### Status`
- `Done.`
- `### Completion Summary`
- concise summary of completed work

### Status
Done.

### Completion Summary
- Added launch-exchange validation run orchestration with a worker entrypoint that accepts target exchanges and persists a validation run lifecycle (`queued` -> `running` -> `succeeded`/`failed`).
- Added persisted validation report shell records per exchange with final verdict fields and pending result buckets for discovery/listing, completeness/timeliness, disagreement/benchmark, and calendar validation slices.
- Added focused tests covering run lifecycle transitions, per-exchange report shell persistence, and same-run-key rerun idempotency to avoid duplicate-state corruption.
