## T32: Provider Price Adapter Interface and Normalized Price Candidate Contract

### Purpose
Define the provider-agnostic adapter boundary for adjusted historical close and official exchange-local opening-price fetches, with a normalized candidate contract.

### Scope
Implement interface/contracts only and fake adapter fixtures for tests. No live provider calls and no persistence.

### Allowed Changes
- Provider adapter interface modules under `project/`
- Price candidate DTO/schema/validator modules
- Contract tests with fake adapter responses

### Forbidden Changes
- Do not call external providers.
- Do not persist provider attempts or candidate values.
- Do not implement source selection logic.

### Inputs
- `task_phase/roadmap_plan.md` Milestone 3 scope
- `design_phase/architecture.md` adapter boundaries
- `design_phase/product_spec.md` FR-12, FR-13, FR-13a, FR-18, FR-19c
- Existing discovery adapter patterns from M2

### Relevant Context
Milestone 3 requires provider-isolated ingestion for two price types: adjusted historical close and official exchange-local unadjusted open. Downstream logging, selection, and validation need one normalized candidate shape.

### Implementation Budget
- Production files: max 2-4 adapter/contract files.
- Behavior count: 1 behavior (normalized provider price-candidate contract).
- Model/API/workflow count: 0 models, 0 APIs, 1 adapter interface boundary.
- Test scope: valid/invalid contract payloads for both price types.

### Expected Output
A stable provider price adapter call shape and normalized candidate schema exist for worker ingestion workflows.

### Acceptance Criteria
- Interface defines request/response shape for historical close and current-day open candidate fetches.
- Candidate contract includes instrument/listing identity, exchange-local trading day, price type, value, currency, and provider metadata fields needed for later audit.
- Contract validation rejects malformed payloads deterministically.

### Test Requirements
- Adapter contract tests with fake responses for valid and invalid candidate payloads.

### Dependencies
- T31

### Stop Condition
Stop when adapter/contract boundary is implemented and tested without persistence or selection behavior.

### Completion Format
Implementation agent must finish task file with:
- `### Status`
- `Done.`
- `### Completion Summary`
- concise summary of completed work


### Status
Done.

### Completion Summary
- Added provider-agnostic price adapter boundary in `project/src/pokus_backend/pricing/adapter.py` with explicit request shape and separate historical-close/current-day-open candidate fetch methods.
- Added normalized `PriceCandidate` contract and deterministic payload validator in `project/src/pokus_backend/pricing/contract.py`, including instrument/listing identity, exchange-local trading day, price type, value, currency, and provider metadata fields.
- Added contract tests with fake adapter payloads in `project/tests/test_price_adapter_contract.py` covering valid normalization and invalid payload rejection for both price types.
