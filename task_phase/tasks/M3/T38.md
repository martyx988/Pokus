## T38: Validation Metrics Slice A - Discovery Quality and Primary-Listing Behavior

### Purpose
Populate validation report metrics for discovery quality and primary/best-listing behavior correctness per launch exchange.

### Scope
Implement only this validation metric slice and report-field population.

### Allowed Changes
- Validation metric service modules for discovery/listing checks
- Validation report updater for this metric group
- Focused tests for metric computation and report writes

### Forbidden Changes
- Do not implement completeness/timeliness/rate-limit metrics here.
- Do not implement benchmark mismatch calculations here.
- Do not change ranking policy behavior.

### Inputs
- T37 validation run/report skeleton
- M2 discovery, ranking, and supported-universe outputs
- `design_phase/product_spec.md` FR-3, FR-5, FR-6, AC-3, AC-4, AC-27

### Relevant Context
Milestone 3 validation must prove discovery coverage is viable and primary/best-listing behavior is correct under the approved ordering policy.

### Implementation Budget
- Production files: max 2-4 validation-metric files.
- Behavior count: 1 behavior (compute and persist discovery/listing validation metrics).
- Model/API/workflow count: 0 new models, 0 APIs, 1 report-update workflow slice.
- Test scope: metric correctness and expected report-field population for pass/fail scenarios.

### Expected Output
Validation report contains discovery-quality and primary-listing behavior results per launch exchange.

### Acceptance Criteria
- Discovery-quality metrics are computed and stored per exchange/run.
- Primary-listing behavior checks are computed and stored per exchange/run.
- Report fields capture pass/fail and supporting evidence for this slice.

### Test Requirements
- Unit/integration tests for metric computation and report updates for both passing and failing data fixtures.

### Dependencies
- T37

### Stop Condition
Stop when this metric slice is persisted and tested without expanding into other validation slices.

### Completion Format
Implementation agent must finish task file with:
- `### Status`
- `Done.`
- `### Completion Summary`
- concise summary of completed work

