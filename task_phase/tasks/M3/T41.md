## T41: Calendar Validation Comparison Output and Custom-Adapter Decision Path

### Purpose
Implement exchange-calendar validation output comparing library calendar results to official reference inputs and recording custom-adapter decisions.

### Scope
Implement calendar-validation comparison and decision recording only.

### Allowed Changes
- Calendar validation service modules
- Validation report fields or linked records for calendar findings
- Focused tests for mismatch detection and decision output

### Forbidden Changes
- Do not build a new custom calendar adapter implementation.
- Do not modify publication-state logic.
- Do not add provider selection behavior.

### Inputs
- T23 calendar resolver
- T37 validation run/report skeleton
- `design_phase/architecture.md` calendar validation guidance
- `design_phase/product_spec.md` FR-56, AC-27

### Relevant Context
Milestone 3 requires explicit output when library calendar coverage mismatches official exchange expectations, including whether custom adapter work is required.

### Implementation Budget
- Production files: max 2-4 calendar-validation files.
- Behavior count: 1 behavior (calendar comparison and decision recording).
- Model/API/workflow count: 0-1 report-field touch, 0 APIs, 1 validation workflow slice.
- Test scope: weekend/holiday mismatch detection and decision-state persistence.

### Expected Output
Validation runs include calendar-comparison findings and a recorded decision on whether custom calendar adapter implementation is needed.

### Acceptance Criteria
- Calendar validation output captures match/mismatch status per exchange and validation window.
- Decision state indicates when current library path is acceptable vs when custom adapter follow-up is required.
- Findings are persisted and attached to validation run evidence.

### Test Requirements
- Tests for matching and mismatching calendar scenarios and resulting decision output.

### Dependencies
- T37

### Stop Condition
Stop when calendar comparison and decision recording are implemented and tested, without implementing custom adapters.

### Completion Format
Implementation agent must finish task file with:
- `### Status`
- `Done.`
- `### Completion Summary`
- concise summary of completed work

### Status
Done.

### Completion Summary
- Added calendar-validation comparison logic that evaluates official reference inputs against launch library calendar decisions per exchange and validation window.
- Persisted `calendar_validation` findings and evidence into validation report buckets, including explicit decision state (`library_acceptable` vs `custom_adapter_required`) and follow-up flag.
- Added focused tests for matching (weekend/holiday) and mismatching scenarios to verify decision output and persisted mismatch evidence.
