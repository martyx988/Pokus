## T34: Candidate Price Value Persistence and Audit-Evidence Records

### Purpose
Persist normalized candidate price values and their audit evidence so source-prioritization outcomes are reproducible.

### Scope
Implement candidate-value storage and audit evidence write path only. No selection policy computation in this task.

### Allowed Changes
- Candidate value persistence modules under `project/`
- Focused model/migration updates for candidate-value/audit rows
- Worker write service for candidate-value ingestion
- Focused persistence tests

### Forbidden Changes
- Do not implement provider reliability score updates.
- Do not implement selected-source ranking algorithm.
- Do not implement validation report generation.

### Inputs
- T32 normalized candidate schema
- T33 provider attempt records
- `design_phase/product_spec.md` FR-19c, AC-24

### Relevant Context
Milestone 3 requires retaining raw candidate values and selection evidence so investigations can reconstruct why a source was chosen later.

### Implementation Budget
- Production files: max 3-5 candidate-value focused files.
- Behavior count: 1 behavior (persist candidate values and audit evidence fields).
- Model/API/workflow count: 1 tightly coupled model family, 0 APIs, 1 worker persistence behavior.
- Test scope: value persistence, dedupe/upsert behavior, and required audit column completeness.

### Expected Output
Candidate price values are stored per business datum with sufficient audit fields for deterministic later source selection explanation.

### Acceptance Criteria
- Candidate values are persisted with price type, value, currency, provider, and exchange-local day context.
- Required audit evidence fields for later selection reasoning are persisted with each candidate set.
- Repeated ingestion of identical candidate sets does not create unstable duplicates.

### Test Requirements
- Persistence tests for candidate upsert/dedup behavior.
- Audit-field completeness tests for stored candidate records.

### Dependencies
- T33

### Stop Condition
Stop when candidate-value/audit persistence is implemented and tested without introducing selection policy logic.

### Completion Format
Implementation agent must finish task file with:
- `### Status`
- `Done.`
- `### Completion Summary`
- concise summary of completed work

