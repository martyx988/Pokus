## T59: Implement Concrete Provider Runtime Path for M3 Validation

### Purpose
Add at least one real provider-backed runtime validation path so Milestone 3 is no longer satisfiable through fixture-only flows.

### Scope
Select and wire one concrete provider integration path for validation execution, including runtime settings and provider-attempt evidence persistence through the normal workflow.

### Allowed Changes
- `project/src/**` provider adapter wiring, validation orchestration, and configuration modules
- `project/README*` or milestone runbook docs for required runtime configuration/commands
- `task_phase/tasks/M3/T59.md` status updates on completion

### Forbidden Changes
- Do not change Milestone 3 source-prioritization policy semantics.
- Do not skip persistence/audit capture in the concrete path.
- Do not add Milestone 4 publication features.

### Inputs
- `task_phase/reviews/M3_workflow_review.md` concrete-evidence blockers
- `design_phase/product_spec.md` FR-17, FR-17b, FR-19, FR-19a, FR-19b, FR-19c, FR-56

### Relevant Context
M3 must prove real runtime provider-validation behavior, not only in-memory seeded fixtures.

### Implementation Budget
- Production files: 2-5 closely related provider/runtime wiring files.
- Behavior count: 1 behavior (concrete provider validation execution path).
- Model/API/workflow count: 0 new APIs, 1 validation runtime workflow, optional 0-1 config surfaces.
- Test scope: concrete provider runtime path and evidence persistence boundary.

### Expected Output
A runnable non-fixture validation path that fetches provider data through a concrete adapter and persists normal validation evidence.

### Acceptance Criteria
- One concrete provider path is selected, wired, and documented.
- Validation workflow can execute against non-fixture input and persist provider attempts/candidate evidence.
- Runtime configuration requirements are explicit and reproducible.

### Test Requirements
- Execute at least one non-mock validation command/path and capture evidence (pass or actionable fail with real runtime trace).

### Dependencies
- T42

### Stop Condition
Stop when concrete provider runtime execution path exists and is evidence-backed.

### Completion Format
Implementation agent must finish task file with:
- `### Status`
- `Done.`
- `### Completion Summary`
- concise summary of completed work
