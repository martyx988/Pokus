## T66: Validate Keyed Discovery Sources A (`EODHD`, `FMP`, `Finnhub`, `Alpha Vantage`)

### Purpose
Implement and execute real discovery/metadata probes for the primary keyed source family most likely to feed the combined universe loader.

### Scope
Add source-specific probe logic and evidence capture for `EODHD`, `FMP`, `Finnhub`, and `Alpha Vantage`, then persist real outcomes and initial verdicts.

### Allowed Changes
- `project/src/pokus_backend/validation/**` source probe modules for this source family
- `project/src/pokus_backend/discovery/**` only where normalized discovery/metadata mapping is directly required
- `project/tests/**` focused probe and mapping tests
- repo-tracked validation artifact files for this source family
- `task_phase/tasks/M3.1/T66.md` status updates on completion

### Forbidden Changes
- Do not implement official-exchange or macro/enrichment probes covered by other tasks.
- Do not assign final combined-loader roles yet.
- Do not broaden into price-loading or publication logic.

### Inputs
- T64
- `task_phase/roadmap_plan.md` Milestone 3.1 acceptance criteria
- `design_phase/product_spec.md` FR-3, FR-4, FR-17a, FR-17b, AC-3, AC-27

### Relevant Context
This family includes the strongest general free/free-tier candidates for launch-universe discovery, but Milestone 3.1 still judges them by real live evidence and complementary value rather than assuming any one source is sufficient by itself.

### Implementation Budget
- Production files: 4-7 closely related probe/mapping files for this source family.
- Behavior count: 1 behavior (live probe family for primary keyed discovery sources).
- Model/API/workflow count: 0 new models, 0 APIs, 1 source-probe workflow covering 4 tightly related sources.
- Test scope: probe normalization, credential handling, evidence persistence, and verdict capture for this family only.

### Expected Output
Real validation evidence and explicit classifications exist for `EODHD`, `FMP`, `Finnhub`, and `Alpha Vantage`.

### Acceptance Criteria
- Each source in this family is exercised through real live requests using environment-based credentials where required.
- Persisted evidence captures auth requirements, observed limits, speed notes, exchange usefulness, and verdict for each source.
- `PSE`-specific findings are explicit for sources where Prague Stock Exchange coverage is uncertain or partial.

### Test Requirements
- Focused probe tests for normalization, auth handling, and error capture.
- Execute at least one real live run covering this source family and record the outcome in validation artifacts.

### Dependencies
- T64

### Stop Condition
Stop when all four sources in this family have live evidence and explicit verdicts.

### Completion Format
Implementation agent must finish task file with:
- `### Status`
- `Done.`
- `### Completion Summary`
- concise summary of completed work

### Status
Done.

### Completion Summary
- Implemented keyed-source family probe package under `project/src/pokus_backend/validation/source_probes/keyed_a/` with live HTTP probes for `EODHD`, `FMP`, `FINNHUB`, and `ALPHA_VANTAGE`, including normalized verdict/role mapping and explicit `PSE` coverage notes.
- Added a keyed-a family runner and CLI command that executes probes via the T64 runtime (`run_live_source_probes`), persists evidence records, and writes repo-tracked validation artifact output.
- Added focused tests `project/tests/test_m31_t66_keyed_a_probes.py` and `project/tests/test_m31_t66_keyed_a_runner.py` covering source normalization, credential alias handling, auth skip behavior, HTTP error capture normalization, and artifact persistence.
- Executed a real live probe run (`m3.1-t66-live-2026-05-02`) and persisted evidence to `project/src/pokus_backend/validation/source_probes/keyed_a/artifacts/t66_keyed_a_latest.json` with explicit per-source verdicts and observed limits.
