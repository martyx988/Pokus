## T72: Wire Docker and Development Rerun Path for Live Universe Loader

### Purpose
Make the Milestone 3.1 live source probes and combined universe loader reproducible in development and Docker.

### Scope
Wire environment examples, Docker development configuration, and concise runtime documentation so the live source-testing and combined-loader flow can be rerun safely.

### Allowed Changes
- `project/docker-compose.dev.yml`
- `project/.env.example`
- `project/README.md`
- `project/DEV_RUNTIME.md`
- minimal `project/src/**` runtime/config wiring only if directly required by the Docker/dev path
- `project/tests/**` focused smoke/config tests if needed
- `task_phase/tasks/M3.1/T72.md` status updates on completion

### Forbidden Changes
- Do not add production deployment infrastructure beyond the development/Docker rerun path.
- Do not add new source integrations.
- Do not broaden into price-loading or publication documentation.

### Inputs
- T71
- `task_phase/roadmap_plan.md` Milestone 3.1 Docker/dev acceptance criteria
- `design_phase/architecture.md` env-secret and VPS/Docker runtime guidance

### Relevant Context
Milestone 3.1 explicitly requires live development testing and a Docker-runnable path. The result must be reproducible with environment-based secrets and must not hardcode provider credentials.

### Implementation Budget
- Production files: 0-2 tiny runtime/config files only if required.
- Behavior count: 1 behavior (reproducible Docker/dev execution path for live universe loading).
- Model/API/workflow count: 0 new models, 0 APIs, 1 runtime execution/documentation path.
- Test scope: config wiring and smoke execution path only.

### Expected Output
Documented Docker and local development paths exist for rerunning live source probes and the combined universe loader.

### Acceptance Criteria
- Development and Docker configuration expose the required environment variables without storing secrets in source control.
- Reproducible commands exist for running live source validation and the combined universe loader.
- Documentation clearly states required credentials, optional credentials, and expected evidence outputs.

### Test Requirements
- Execute at least one Docker or documented dev smoke path and record whether it passed or produced an actionable environment/setup failure.

### Dependencies
- T71

### Stop Condition
Stop when live reruns are reproducible in Docker/dev and the required credential/config contract is documented.

### Completion Format
Implementation agent must finish task file with:
- `### Status`
- `Done.`
- `### Completion Summary`
- concise summary of completed work

### Status
Done.

### Completion Summary
- Updated `Project/.env.example` with the Milestone 3.1 live rerun credential contract, separating required keyed source credentials from optional `OPENFIGI_API_KEY`, while keeping secrets environment-only.
- Extended `Project/docker-compose.dev.yml` with a reusable `dev-worker` service wired for runtime reruns against Docker PostgreSQL using the existing worker command surface.
- Added explicit local and Docker rerun commands in `Project/README.md` and `Project/DEV_RUNTIME.md` for `--run-live-source-probes` and `--run-combined-universe-loader`, including expected evidence outputs.
- Executed documented Docker smoke path: `docker compose -f docker-compose.dev.yml run --rm dev-worker --run-combined-universe-loader`; result was actionable setup failure (`.env` missing), matching documented prerequisite (`Copy-Item .env.example .env`).
