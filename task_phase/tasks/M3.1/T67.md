## T67: Validate Keyed Discovery Sources B (`Tiingo`, `Marketstack`, `Polygon`, `Twelve Data`)

### Purpose
Implement and execute real discovery/metadata probes for the auxiliary keyed API family that may contribute partial or exchange-specific value.

### Scope
Add source-specific probe logic and evidence capture for `Tiingo`, `Marketstack`, `Polygon`, and `Twelve Data`, then persist real outcomes and initial verdicts.

### Allowed Changes
- `project/src/pokus_backend/validation/**` source probe modules for this source family
- `project/src/pokus_backend/discovery/**` only where normalized discovery/metadata mapping is directly required
- `project/tests/**` focused probe and mapping tests
- repo-tracked validation artifact files for this source family
- `task_phase/tasks/M3.1/T67.md` status updates on completion

### Forbidden Changes
- Do not finalize the combined loader in this task.
- Do not implement official-exchange probe logic covered by T68.
- Do not add price-loading or publication behavior.

### Inputs
- T64
- `task_phase/roadmap_plan.md` Milestone 3.1 source list and role-based evaluation approach
- `design_phase/architecture.md` aggregate-quality and exchange-specific validation guidance

### Relevant Context
This source family may be strong only in certain exchanges, metadata fields, or fallback roles. The task must capture real partial value and explicit limits rather than treating partial usefulness as automatic rejection.

### Implementation Budget
- Production files: 4-7 closely related probe/mapping files for this source family.
- Behavior count: 1 behavior (live probe family for auxiliary keyed discovery sources).
- Model/API/workflow count: 0 new models, 0 APIs, 1 source-probe workflow covering 4 tightly related sources.
- Test scope: probe normalization, credential handling, evidence persistence, and verdict capture for this family only.

### Expected Output
Real validation evidence and explicit classifications exist for `Tiingo`, `Marketstack`, `Polygon`, and `Twelve Data`.

### Acceptance Criteria
- Each source in this family is exercised through real live requests using environment-based credentials where required.
- Persisted evidence captures auth requirements, observed limits, speed notes, exchange usefulness, and verdict for each source.
- Exchange-specific restrictions, such as US-only or weak-`PSE` behavior, are recorded explicitly rather than implied.

### Test Requirements
- Focused probe tests for normalization, auth handling, and error capture.
- Execute at least one real live run covering this source family and record the outcome in validation artifacts.

### Dependencies
- T64

### Stop Condition
Stop when all four sources in this family have live evidence and explicit verdicts.

### Completion Format
Implementation agent must finish task file with:
- `### Status`
- `Done.`
- `### Completion Summary`
- concise summary of completed work

### Status
Done.

### Completion Summary
- Implemented the keyed-B live probe family under `validation/source_probes/keyed_b` for `TIINGO`, `MARKETSTACK`, `POLYGON`, and `TWELVE_DATA`, including auth-aware probe logic, env secret alias handling, and explicit US/PSE coverage-limit notes.
- Added a keyed-B live run CLI/workflow that executes probes through the generic live source runner and persists repo-tracked evidence JSON artifacts for T67.
- Added focused `test_m31_t67_*` tests covering probe registry/auth wiring, payload/error normalization behavior, and evidence artifact persistence.
- Executed a real live keyed-B probe run and persisted evidence to `src/pokus_backend/validation/source_probes/keyed_b/artifacts/t67_keyed_b_live_probe_evidence.json`.
