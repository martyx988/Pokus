## T73: Milestone 3.1 Integration Gate and Live Universe Evidence

### Purpose
Validate Milestone 3.1 end to end: all listed sources tested, verdicts captured, combined loader integrated, and Docker/dev rerun path documented.

### Scope
Add focused Milestone 3.1 integration tests and live evidence commands. Allow only tiny defect fixes required by gate assertions.

### Allowed Changes
- Milestone-focused integration tests under `project/tests/`
- repo-tracked validation artifact summaries for Milestone 3.1 completion evidence
- `task_phase/tasks/M3.1/T73.md` status updates on completion
- tiny defect fixes directly required by failing M3.1 gate assertions

### Forbidden Changes
- Do not add Milestone 4 price-loading or publication features.
- Do not add new source integrations outside T65-T69.
- Do not turn the gate into a catch-all implementation task for skipped Milestone 3.1 work.

### Inputs
- Outputs of T63-T72
- Milestone 3.1 acceptance criteria from `task_phase/roadmap_plan.md`
- `design_phase/product_spec.md` FR-3, FR-4, FR-8, FR-9a, FR-9b, FR-17a, FR-17b, FR-56, AC-3, AC-27

### Relevant Context
Milestone 3.1 is complete only when the project can show real source-testing evidence for every listed source, explicit source verdicts, a combined loader wired into the project, and a reproducible rerun path for development and Docker.

### Implementation Budget
- Production files: 0-1 tiny defect-fix files only if needed by gate failures.
- Behavior count: 1 behavior (integration validation of Milestone 3.1 live universe-loading chain).
- Model/API/workflow count: 0 new models, 0 APIs, 1 integrated universe-loader validation workflow.
- Test scope: full M3.1 chain only.

### Expected Output
Runnable Milestone 3.1 gate proving complete live-source evaluation coverage, explicit role/classification outputs, integrated loader execution, and rerun documentation.

### Acceptance Criteria
- Integration assertions cover evidence persistence, per-source verdict capture, role-matrix derivation, combined-loader execution, and rerun-path documentation presence.
- Completion evidence explicitly accounts for every listed source, including sources classified as `not for universe loader`, `validation only`, or `reject`.
- Gate includes at least one real live execution path for source validation and at least one real combined-loader execution path, with pass or actionable fail evidence recorded.

### Test Requirements
- Execute focused M3.1 integration gate command(s).
- Execute at least one live source-validation command and one combined-loader runtime command, then record pass/fail/skip conditions and environment assumptions in the completion summary.

### Dependencies
- T65
- T66
- T67
- T68
- T69
- T70
- T71
- T72

### Stop Condition
Stop when Milestone 3.1 evidence exists and no acceptance gap remains unaddressed.

### Completion Format
Implementation agent must finish task file with:
- `### Status`
- `Done.`
- `### Completion Summary`
- concise summary of completed work

### Status
Done.

### Completion Summary
- Added focused Milestone 3.1 gate coverage at `project/tests/test_m31_t73_milestone_gate.py` asserting full listed-source accounting, explicit verdict-family coverage, rerun-command documentation presence in `README.md` and `DEV_RUNTIME.md`, and tracked live command evidence payload shape.
- Added repo-tracked milestone evidence summary artifact at `project/src/pokus_backend/validation/artifacts/m3_1_t73_gate_summary.json` capturing environment assumptions plus one real live-source-validation command result and one real combined-loader runtime command result.
- Executed T73 live commands against local sqlite runtime and recorded actionable failures (sqlite migration incompatibility and downstream missing-table runtime errors) in the summary artifact without expanding scope beyond gate evidence/assertions.
