## T69: Validate Macro and Enrichment Sources (`FRED`, `DBnomics`, `IMF`, `World Bank`)

### Purpose
Execute real probes for the macro/enrichment sources required by Milestone 3.1 and classify their actual usefulness to instrument-universe loading.

### Scope
Add probe logic and evidence capture for `FRED`, `DBnomics`, `IMF`, and `World Bank`, then persist real outcomes and explicit `not for universe loader`, `validation only`, or other verdicts from evidence.

### Allowed Changes
- `project/src/pokus_backend/validation/**` source probe modules for this source family
- `project/tests/**` focused probe tests
- repo-tracked validation artifact files for this source family
- `task_phase/tasks/M3.1/T69.md` status updates on completion

### Forbidden Changes
- Do not force these sources into the combined loader if live evidence shows they are not useful for universe loading.
- Do not broaden into FX, macro, or analytics features outside Milestone 3.1.
- Do not add price-loading or publication behavior.

### Inputs
- T64
- `task_phase/roadmap_plan.md` Milestone 3.1 source list and classification rules
- `design_phase/architecture.md` aggregate-evidence and source-diversity principles

### Relevant Context
These sources must be tested for real, but Milestone 3.1 focuses on instrument-universe loading and metadata discovery. If a source does not materially help that loader, the correct output is explicit evidence-backed classification, not forced production use.

### Implementation Budget
- Production files: 3-6 closely related probe files for this source family.
- Behavior count: 1 behavior (live probe family for macro/enrichment sources).
- Model/API/workflow count: 0 new models, 0 APIs, 1 source-probe workflow covering 4 tightly related sources.
- Test scope: probe execution, evidence persistence, and out-of-scope verdict capture for this family only.

### Expected Output
Real validation evidence and explicit classifications exist for `FRED`, `DBnomics`, `IMF`, and `World Bank`.

### Acceptance Criteria
- Each source in this family is exercised through real live requests or equivalent live interaction.
- Persisted evidence captures observed usefulness or lack of usefulness for instrument-universe loading.
- Sources that do not materially help the universe loader are explicitly classified rather than silently ignored.

### Test Requirements
- Focused probe tests for execution and evidence capture.
- Execute at least one real live run covering this source family and record the outcome in validation artifacts.

### Dependencies
- T64

### Stop Condition
Stop when all four sources in this family have live evidence and explicit verdicts.

### Completion Format
Implementation agent must finish task file with:
- `### Status`
- `Done.`
- `### Completion Summary`
- concise summary of completed work
