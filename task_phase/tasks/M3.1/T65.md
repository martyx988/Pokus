## T65: Validate Non-Keyed Discovery Sources (`yfinance`, `Stooq`, `AkShare`)

### Purpose
Implement and execute real discovery/metadata probes for the non-keyed and experimental sources in the Milestone 3.1 source list.

### Scope
Add source-specific probe logic and evidence capture for `yfinance`, `Stooq`, and `AkShare`, then record real outcomes and initial verdicts for each.

### Allowed Changes
- `project/src/pokus_backend/validation/**` source probe modules for this source family
- `project/src/pokus_backend/discovery/**` only where normalized discovery/metadata mapping is directly required
- `project/tests/**` focused probe and mapping tests
- repo-tracked validation artifact files for this source family
- `task_phase/tasks/M3.1/T65.md` status updates on completion

### Forbidden Changes
- Do not implement keyed-provider logic covered by later tasks.
- Do not finalize the combined loader.
- Do not add price-record persistence or publication behavior.

### Inputs
- T64
- `task_phase/roadmap_plan.md` Milestone 3.1 source list and classification rules
- `design_phase/architecture.md` source-diversity and aggregate-quality guidance

### Relevant Context
`yfinance` is useful for bulk research and comparison but is not assumed to be a trust anchor. `Stooq` and `AkShare` may still contribute useful partial discovery or validation value even if they are weak standalone choices, so the task must capture real evidence rather than reject them prematurely.

### Implementation Budget
- Production files: 3-6 closely related probe/mapping files for this source family.
- Behavior count: 1 behavior (live probe family for non-keyed discovery sources).
- Model/API/workflow count: 0 new models, 0 APIs, 1 source-probe workflow covering 3 tightly related sources.
- Test scope: probe normalization, evidence persistence, and verdict capture for this family only.

### Expected Output
Real validation evidence and explicit classifications exist for `yfinance`, `Stooq`, and `AkShare`.

### Acceptance Criteria
- Each source in this family is exercised through a real live request or equivalent live provider interaction.
- Persisted evidence records availability, speed notes, exchange usefulness, and verdict for each source.
- Any source classified as weak or out of scope still retains explicit evidence and reasoning rather than being silently skipped.

### Test Requirements
- Focused probe tests for normalization and error capture.
- Execute at least one real live run covering this source family and record the outcome in validation artifacts.

### Dependencies
- T64

### Stop Condition
Stop when all three sources in this family have live evidence and explicit verdicts.

### Completion Format
Implementation agent must finish task file with:
- `### Status`
- `Done.`
- `### Completion Summary`
- concise summary of completed work

### Status
Done.

### Completion Summary
- Implemented a dedicated non-keyed live probe family under `project/src/pokus_backend/validation/source_probes/non_keyed/` for `YFINANCE`, `STOOQ`, and `AKSHARE`, including source-specific live request handling, normalization, and explicit verdict/role assignment payloads.
- Added focused tests in `project/tests/test_m31_t65_non_keyed_source_probes.py` covering source normalization, persistence through the generic live probe runner, and probe-level error capture without silent skips.
- Executed a real live run on May 2, 2026 (UTC) via:
  - `PYTHONPATH=src python -m pokus_backend.validation.source_probes.non_keyed.run_non_keyed_live_probe_family --run-key m3.1-t65-live-20260502 --database-url sqlite+pysqlite:///./.evidence/m31_t65_non_keyed_live_probe.sqlite3 --artifact-path src/pokus_backend/validation/source_probes/non_keyed/artifacts/m3_1_t65_live_probe_results.json`
  - Result: `sources=3 succeeded=3 skipped=0 failed=0`
  - Persisted verdicts: `YFINANCE -> fallback_only`, `STOOQ -> validation_only` (manual apikey/captcha gate observed), `AKSHARE -> fallback_only`
