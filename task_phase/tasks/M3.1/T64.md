## T64: Run Live Universe Source Probes with Secret-Aware Worker Command

### Purpose
Add one reusable runtime path for executing live universe-source probes with environment-based secrets and durable evidence capture.

### Scope
Implement the generic command/orchestration layer that runs selected source probes, handles missing credentials safely, and persists per-source execution results.

### Allowed Changes
- `project/src/pokus_backend/validation/**` live probe orchestration modules
- `project/src/pokus_backend/settings.py`
- `project/src/pokus_backend/worker.py`
- `project/tests/**` focused runtime/config tests
- `task_phase/tasks/M3.1/T64.md` status updates on completion

### Forbidden Changes
- Do not implement source-specific probe logic beyond what is required for the generic runner boundary.
- Do not assign final source roles or final combined-loader decisions yet.
- Do not add price-loading workflows.

### Inputs
- T63
- `task_phase/roadmap_plan.md` Milestone 3 and 3.1 gate requirements
- `design_phase/architecture.md` worker-runtime and env-secret guidance

### Relevant Context
Milestone 3.1 needs real live testing, not paper evaluation. That requires a worker-accessible runtime command that can execute source probes in development and Docker while keeping provider keys in environment variables rather than source control.

### Implementation Budget
- Production files: 2-5 closely related runtime/config files.
- Behavior count: 1 behavior (run live universe-source probes through a reusable worker command).
- Model/API/workflow count: 0 new models, up to 1 worker command surface, 1 validation runtime workflow.
- Test scope: runner dispatch, missing-secret handling, and evidence persistence boundary.

### Expected Output
A reproducible worker/runtime command can execute selected universe-source probes live and persist evidence using T63 storage.

### Acceptance Criteria
- Worker/runtime surface accepts source selection and executes live probe runs through a reusable orchestration path.
- Missing or optional provider secrets produce explicit, auditable skip/fail outcomes rather than silent success.
- Probe execution persists source-validation records through the normal workflow.

### Test Requirements
- Focused runtime/config tests for source selection, missing-secret behavior, and evidence persistence.

### Dependencies
- T63

### Stop Condition
Stop when live probe execution is possible without source-specific implementation shortcuts in later tasks.

### Completion Format
Implementation agent must finish task file with:
- `### Status`
- `Done.`
- `### Completion Summary`
- concise summary of completed work
