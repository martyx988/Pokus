## T68: Validate Official and Symbology Sources (`Nasdaq Trader`, `NYSE`, `PSE`/`PSE EDGE`, `OpenFIGI`, `Nasdaq Data Link`)

### Purpose
Implement and execute real discovery, reference, and symbology probes for the official-source and normalization family most important to launch-universe trust.

### Scope
Add source-specific probe logic and evidence capture for official `NASDAQ` sources, official `NYSE` sources, official `PSE`/`PSE EDGE`, `OpenFIGI`, and `Nasdaq Data Link`, then persist real outcomes and initial verdicts.

### Allowed Changes
- `project/src/pokus_backend/validation/**` source probe modules for this source family
- `project/src/pokus_backend/discovery/**` only where normalized discovery/metadata or identifier mapping is directly required
- `project/tests/**` focused probe and mapping tests
- repo-tracked validation artifact files for this source family
- `task_phase/tasks/M3.1/T68.md` status updates on completion

### Forbidden Changes
- Do not implement macro/enrichment probes covered by T69.
- Do not finalize the combined loader in this task.
- Do not add price-loading or publication behavior.

### Inputs
- T64
- `design_phase/architecture.md` universe management, source-diversity, and `PSE` trust-bar guidance
- `design_phase/product_spec.md` FR-3, FR-5, FR-9a, FR-9b, FR-56a, FR-56b, AC-1a, AC-24a, AC-27

### Relevant Context
Official exchange sources and symbology sources may not all be primary loader candidates, but they are critical for trustworthy universe discovery, identifier normalization, and `PSE` validation. The task must prove their real usefulness and limits through live evidence.

### Implementation Budget
- Production files: 4-8 closely related probe/mapping files for this source family.
- Behavior count: 1 behavior (live probe family for official/reference/symbology sources).
- Model/API/workflow count: 0 new models, 0 APIs, 1 source-probe workflow covering 5 tightly related source groups.
- Test scope: probe normalization, identifier mapping, evidence persistence, and verdict capture for this family only.

### Expected Output
Real validation evidence and explicit classifications exist for the official/reference/symbology source family, including `PSE`-specific findings.

### Acceptance Criteria
- Each source group in this family is exercised through a real live request, download, or equivalent live interaction where technically applicable.
- Persisted evidence captures discovery usefulness, identifier usefulness, exchange-specific coverage, and verdict for each source.
- `PSE` and `PSE EDGE` findings are explicit enough to support later launch-go/no-go investigation.

### Test Requirements
- Focused probe tests for normalization, identifier handling, and error capture.
- Execute at least one real live run covering this source family and record the outcome in validation artifacts.

### Dependencies
- T64

### Stop Condition
Stop when all sources in this family have live evidence and explicit verdicts.

### Completion Format
Implementation agent must finish task file with:
- `### Status`
- `Done.`
- `### Completion Summary`
- concise summary of completed work

### Status
Done.

### Completion Summary
- Implemented a dedicated official/symbology live probe family under `validation/source_probes/official_symbology` for `NASDAQ_TRADER`, `NYSE`, `PSE/PSE EDGE`, `OPENFIGI`, and `NASDAQ_DATA_LINK`, including source-code normalization and per-source verdict/role decisions.
- Added a reusable runner and artifact writer that executes this source family, persists `source_validation_record` evidence through `run_live_source_probes`, and writes a repo-tracked JSON artifact with live outcomes.
- Added focused `test_m31_t68_*` coverage for source normalization aliases, key-error classification behavior, persistence flow, and artifact content.
- Executed a real live run for all five source groups and captured results in `official_symbology/artifacts/m3_1_t68_live_probe_results.json`.
