## T74: Fix M3.1 Runtime Migration Baseline for Gate Execution

### Purpose
Resolve the migration/runtime baseline issue that blocks Milestone 3.1 concrete live command evidence.

### Scope
Implement the smallest safe fix so the Milestone 3.1 gate execution context can run migrations successfully and create required schema objects for live probe and combined-loader runtime commands.

### Allowed Changes
- `project/migrations/**` migration scripts directly related to the failing schema step
- `project/src/pokus_backend/db.py` or tightly related migration bootstrap helpers only if directly required
- `project/tests/**` focused migration/runtime baseline tests
- `task_phase/tasks/M3.1/T74.md` status updates on completion

### Forbidden Changes
- Do not add new source integrations.
- Do not expand milestone scope beyond migration/runtime baseline repair.
- Do not modify unrelated publication or signal workflows.

### Inputs
- `task_phase/reviews/M3.1_workflow_review.md` failed checks
- `task_phase/tasks/M3.1/T73.md` runtime failure evidence
- `project/src/pokus_backend/validation/artifacts/m3_1_t73_gate_summary.json`

### Relevant Context
M3.1 is blocked because live gate commands cannot run after migration failure in the reviewed runtime context. This task is a focused unblocker for that concrete evidence path.

### Implementation Budget
- Production files: 1-3 closely related migration/runtime bootstrap files.
- Behavior count: 1 behavior (make gate migration baseline runnable in the reviewed context).
- Model/API/workflow count: 0 new domain models, 0 APIs, 1 migration/runtime baseline path.
- Test scope: migration success and required-table presence for downstream M3.1 runtime commands.

### Expected Output
Migration command succeeds in the targeted runtime context and required schema tables exist for live probe and combined-loader execution.

### Acceptance Criteria
- The previously failing migration step is fixed for the M3.1 gate execution context.
- Running migration command succeeds end-to-end in the documented test context.
- Required tables used by `--run-live-source-probes` and `--run-combined-universe-loader` exist after migration.

### Test Requirements
- Add/execute focused migration runtime test(s) that reproduce prior failure and prove the fix.

### Dependencies
- T73

### Stop Condition
Stop when migration baseline is fixed and validated for the M3.1 gate execution context.

### Completion Format
Implementation agent must finish task file with:
- `### Status`
- `Done.`
- `### Completion Summary`
- concise summary of completed work

### Status
Done.

### Completion Summary
- Added sqlite-focused baseline handling in `pokus_backend.db --migrate` so the gate runtime context creates schema via ORM metadata and seeds launch baseline records, avoiding the sqlite-incompatible alembic path seen in T73 evidence.
- Added focused regression coverage in `project/tests/test_m31_t74_migration_baseline.py` that reproduces the prior failing command context and verifies `listing` and `source_validation_record` exist after migration.
- Executed the new T74 test successfully to validate migration command success and required M3.1 runtime table presence.
