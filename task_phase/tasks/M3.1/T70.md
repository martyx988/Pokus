## T70: Build Combined Universe Source Classification Matrix and Role Selector

### Purpose
Turn per-source live evidence into one auditable classification matrix and runtime role-selection rule set for the combined universe loader.

### Scope
Implement the logic that assigns each tested source its milestone verdict and runtime role, based on persisted evidence from T65-T69 rather than manual one-off judgment.

### Allowed Changes
- `project/src/pokus_backend/validation/**` classification/selection modules
- `project/src/pokus_backend/discovery/**` source-role selection helpers only where directly required
- `project/tests/**` focused classification/selection tests
- repo-tracked validation artifact files for combined classification outputs
- `task_phase/tasks/M3.1/T70.md` status updates on completion

### Forbidden Changes
- Do not run the full combined loader yet.
- Do not add new provider integrations beyond what earlier probe tasks already covered.
- Do not add price-loading or publication behavior.

### Inputs
- T65
- T66
- T67
- T68
- T69
- `design_phase/product_spec.md` FR-17a, FR-17b, NFR-11, NFR-11a, AC-27

### Relevant Context
Milestone 3.1 success is judged by the quality of the combined algorithm, not by requiring every source to stand alone. The system therefore needs one explicit, auditable way to promote, demote, or restrict sources into roles like primary discovery, fallback, validation-only, symbology helper, or not-for-loader.

### Implementation Budget
- Production files: 2-5 closely related classification/selection files.
- Behavior count: 1 behavior (derive combined source roles from persisted live evidence).
- Model/API/workflow count: 0 new models, 0 APIs, 1 classification workflow.
- Test scope: classification/rule outcomes from representative evidence inputs.

### Expected Output
A deterministic combined source-classification matrix and role selector exists for the universe loader.

### Acceptance Criteria
- The system can derive explicit verdicts and runtime roles from persisted live evidence.
- Partial-usefulness sources can be assigned constrained roles without being forced into primary-loader status.
- Combined classification output is durable and explainable enough to support later investigation.

### Test Requirements
- Focused classification tests covering promote/fallback/validation-only/not-for-loader/reject outcomes and role assignment.

### Dependencies
- T65
- T66
- T67
- T68
- T69

### Stop Condition
Stop when the source-role matrix is deterministic, evidence-backed, and ready to drive the concrete loader.

### Completion Format
Implementation agent must finish task file with:
- `### Status`
- `Done.`
- `### Completion Summary`
- concise summary of completed work

### Status
Done.

### Completion Summary
- Implemented a deterministic combined source-classification workflow that derives runtime source roles from persisted T65-T69 live evidence and normalizes verdict-to-role outcomes for `promote`, `fallback_only`, `validation_only`, `not_for_universe_loader`, and `reject`.
- Added a runtime role selector for loader-eligible source picking and exported the new classification/selection helpers from the validation package.
- Added focused T70 tests covering required verdict outcomes, role assignment behavior, deterministic selection, and auditable matrix artifact persistence from real milestone evidence inputs.
- Generated and tracked combined classification output artifact at `Project/src/pokus_backend/validation/artifacts/m3_1_t70_combined_source_matrix.json`.
