## T71: Integrate Concrete Combined Universe Loader into Worker Flow

### Purpose
Replace the current placeholder universe-load path with a real combined loader that uses the Milestone 3.1 source-role matrix and existing discovery/universe modules.

### Scope
Implement the concrete runtime flow that calls selected discovery/metadata sources, merges their outputs according to T70 roles, and persists supported-universe inputs through the normal project path.

### Allowed Changes
- `project/src/pokus_backend/discovery/**` concrete loader orchestration and merge wiring
- `project/src/pokus_backend/validation/**` only where role-selection handoff is directly required
- `project/src/pokus_backend/worker.py`
- `project/src/pokus_backend/settings.py` only if directly required for runtime wiring
- `project/tests/**` focused runtime/integration tests for the loader boundary
- `task_phase/tasks/M3.1/T71.md` status updates on completion

### Forbidden Changes
- Do not implement price-loading behavior.
- Do not add publication, readiness, or app API changes.
- Do not bypass existing persistence/audit paths with one-off fixture-only shortcuts.

### Inputs
- T70
- `design_phase/architecture.md` universe management module responsibilities
- `design_phase/product_spec.md` FR-3 through FR-11, AC-3 through AC-5, AC-24a

### Relevant Context
Milestone 3.1 must end with a real loader in the project, not only reports. That loader should produce supported-universe and metadata outputs through the normal persistence flow while staying limited to universe loading and metadata discovery.

### Implementation Budget
- Production files: 3-6 closely related loader/runtime files.
- Behavior count: 1 behavior (run concrete combined instrument-universe loader through the worker/project boundary).
- Model/API/workflow count: 0 new models, up to 1 runtime command surface, 1 integrated universe-loader workflow.
- Test scope: concrete loader boundary, merge behavior, and persistence outputs.

### Expected Output
A real integrated combined universe loader replaces the placeholder path and writes auditable universe-loading outputs through the project runtime.

### Acceptance Criteria
- Combined loader runs through the normal project flow rather than isolated helper-only execution.
- Loader uses source-role outputs from T70 instead of hardcoding one-source behavior.
- Loader persists discovery/metadata results into the existing supported-universe path and keeps evidence needed for investigation.

### Test Requirements
- Focused runtime/integration tests for loader execution, merge behavior, and persistence output.
- Execute at least one non-fixture runtime path for the combined loader.

### Dependencies
- T70

### Stop Condition
Stop when the project contains a real combined universe-loader runtime path and the placeholder path is no longer the active implementation.

### Completion Format
Implementation agent must finish task file with:
- `### Status`
- `Done.`
- `### Completion Summary`
- concise summary of completed work
