## T75: Re-run M3.1 Concrete Runtime Gate and Refresh Closure Evidence

### Purpose
Re-run Milestone 3.1 concrete runtime validation after T74 and capture passing closure evidence for workflow-review pass.

### Scope
Execute focused M3.1 gate commands and tests again, store refreshed evidence artifacts, and update milestone review inputs accordingly.

### Allowed Changes
- `project/tests/**` focused gate/rerun tests if tiny fixes are required
- `project/src/pokus_backend/validation/artifacts/**` refreshed gate evidence artifacts
- `task_phase/tasks/M3.1/T75.md` status updates on completion
- tiny defect fixes only if directly required by rerun failures

### Forbidden Changes
- Do not add new source families or broaden discovery scope.
- Do not add new milestone-level features beyond closing gate evidence.
- Do not bypass concrete runtime commands with fixture-only substitutions.

### Inputs
- T74 outputs
- `task_phase/reviews/M3.1_workflow_review.md`
- `task_phase/tasks/M3.1/T73.md`

### Relevant Context
Milestone 3.1 cannot be marked complete until concrete runtime commands and gate evidence pass rather than fail with actionable blockers.

### Implementation Budget
- Production files: 0-1 tiny fix file only if rerun requires it.
- Behavior count: 1 behavior (successful M3.1 runtime gate rerun and evidence refresh).
- Model/API/workflow count: 0 new models, 0 APIs, 1 gate rerun workflow.
- Test scope: milestone gate assertions and concrete runtime command outcomes.

### Expected Output
Updated gate evidence showing successful migration, successful live probe command, and successful combined loader runtime command in the reviewed context.

### Acceptance Criteria
- Gate tests pass with updated evidence.
- Live source-probe runtime command succeeds in the documented rerun context.
- Combined universe-loader runtime command succeeds in the documented rerun context.
- Task completion summary records exact commands and results for workflow-review consumption.

### Test Requirements
- Execute focused M3.1 gate test command(s).
- Execute live runtime commands and record pass results.

### Dependencies
- T74

### Stop Condition
Stop when M3.1 gate evidence is refreshed and no runtime blocker remains for workflow-review pass.

### Completion Format
Implementation agent must finish task file with:
- `### Status`
- `Done.`
- `### Completion Summary`
- concise summary of completed work

### Status
Done.

### Completion Summary
- Re-ran the M3.1 concrete runtime gate in the local reviewed context with `PYTHONPATH=src` and `DATABASE_URL=sqlite+pysqlite:///./.evidence/t75_gate_runtime.sqlite3`; migration command passed: `python -m pokus_backend.db --migrate` (`database-migrate-ok`, exit code 0).
- Applied one tiny defect fix required by rerun failure: wired `worker --run-live-source-probes` to use the existing non-keyed probe registry so `YFINANCE` runs concrete probe logic instead of always failing as unregistered.
- Re-ran live runtime commands successfully and refreshed closure evidence artifact `project/src/pokus_backend/validation/artifacts/m3_1_t73_gate_summary.json` with pass results:
  - `python -m pokus_backend.worker --run-live-source-probes --source-probe-run-key m31-t75-live-local-2026-05-02 --source-probe-sources YFINANCE` (pass, exit code 0)
  - `python -m pokus_backend.worker --run-combined-universe-loader` (pass, exit code 0)
- Executed focused M3.1 gate/rerun tests and confirmed pass:
  - `python -m pytest tests/test_m31_t74_migration_baseline.py tests/test_m31_t73_milestone_gate.py tests/test_worker_live_source_probe_command.py -q` -> `4 passed`.
