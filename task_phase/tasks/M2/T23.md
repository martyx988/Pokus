## T23: Exchange Calendar Expected-Day and Market-Closed Resolver

### Purpose
Provide worker-accessible calendar resolver for expected trading day vs market closed decisions per exchange/day.

### Scope
Implement calendar abstraction use for M2 workflows and market-closed classification only.

### Allowed Changes
- Calendar service module(s) under `project/`
- Minimal configuration wiring for launch exchanges
- Focused tests for weekends/holidays/expected trading days

### Forbidden Changes
- Do not implement provider validation flow.
- Do not implement price loading or publication rules.
- Do not implement delisting denominator exclusions in this task.

### Inputs
- Milestone 1 calendar abstraction foundation
- `design_phase/architecture.md` calendar requirements
- `design_phase/product_spec.md` FR-34 expected-day semantics

### Relevant Context
M2 requires market-closed handling and discovery cadence that does not create false failures on closed days.

### Implementation Budget
- Production files: max 2-3 calendar-focused files.
- Behavior count: 1 behavior (exchange/day open-or-closed resolution).
- Model/API/workflow count: 0 models, 0 APIs, 1 workflow utility.
- Test scope: calendar classification accuracy for launch exchanges across representative dates.

### Expected Output
Worker-facing function/service can answer whether exchange/day is expected trading day or market closed.

### Acceptance Criteria
- Resolver returns market-closed for weekends and known holidays.
- Resolver returns expected trading day for normal open sessions.
- Resolver output is consumable by discovery workflow.

### Test Requirements
- Unit tests for at least weekend, holiday, and normal trading day scenarios per launch exchange.

### Dependencies
- T22.

### Stop Condition
Stop when calendar resolver is implemented and tested for M2 needs.

### Completion Format
Implementation agent must finish task file with:
- `### Status`
- `Done.`
- `### Completion Summary`
- concise summary of completed work

### Status
Done.

### Completion Summary
- Implemented launch exchange calendar resolution with weekend and known-holiday classification for `NYSE`, `NASDAQ`, and `PSE`, plus a worker-consumable service builder.
- Kept resolver behavior scoped to expected-trading-day vs market-closed decisions without adding provider validation or load/publication logic.
- Added focused unit tests covering weekend, holiday, and normal trading day scenarios across launch exchanges.