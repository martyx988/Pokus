## T57: Fix M2 Metadata Wiring for `load_jobs` Foreign-Key Resolution

### Purpose
Resolve the concrete metadata/bootstrap defect that prevents Milestone 2 integration gate setup from resolving `exchange_day_load.job_id -> load_jobs.id`.

### Scope
Repair model import/registration or metadata bootstrapping so all FK target tables required by M2 gate are present at runtime.

### Allowed Changes
- `project/src/**` model registry/bootstrap modules tied to SQLAlchemy metadata assembly
- `project/tests/**` bootstrap helpers only if needed for accurate production-like metadata load
- `task_phase/tasks/M2/T57.md` status updates on completion

### Forbidden Changes
- Do not change Milestone 2 domain semantics.
- Do not remove the foreign key as a shortcut.
- Do not expand into Milestone 3 provider-validation behavior.

### Inputs
- `task_phase/reviews/M2_workflow_review.md` failed-check evidence
- `tests/test_m2_integration_gate.py`

### Relevant Context
M2 is blocked by setup-time metadata failure, so milestone validation cannot currently run to completion.

### Implementation Budget
- Production files: 1-3 closely related model/bootstrap files.
- Behavior count: 1 behavior (complete metadata registration for M2 gate).
- Model/API/workflow count: up to 1 metadata assembly path; 0 new APIs.
- Test scope: metadata bootstrap and M2 gate startup.

### Expected Output
M2 test bootstrap resolves all referenced tables including `load_jobs` and proceeds into integration assertions.

### Acceptance Criteria
- `NoReferencedTableError` for `load_jobs` is eliminated.
- FK relationship remains intact and valid.
- Fix remains limited to metadata/bootstrap wiring.

### Test Requirements
- Run focused M2 integration gate command and capture startup success/failure evidence.

### Dependencies
- T31

### Stop Condition
Stop when M2 gate setup resolves FK metadata successfully.

### Completion Format
Implementation agent must finish task file with:
- `### Status`
- `Done.`
- `### Completion Summary`
- concise summary of completed work

### Status
Done.

### Completion Summary
- Added production metadata registration for `load_jobs` on shared SQLAlchemy `Base.metadata` in `load_tracking_models` so FK targets for `exchange_day_load.job_id` and `instrument_load_outcome.job_id` resolve during bootstrap.
- Verified M2 integration gate setup now succeeds and proceeds into assertions with no `NoReferencedTableError`.
