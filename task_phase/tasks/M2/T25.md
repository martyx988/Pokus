## T25: Discovery Candidate Persistence and Stable Identifier Capture

### Purpose
Persist discovered candidate instruments/listings and capture stable provider/reference identifiers where available.

### Scope
Implement write path from normalized discovery candidates to candidate listing records and identifier records.

### Allowed Changes
- Candidate listing and identifier persistence modules
- Worker-side discovery ingestion write service
- Focused migration/model updates strictly for candidate + identifier storage
- Focused persistence tests

### Forbidden Changes
- Do not implement final best-listing selection.
- Do not set supported-universe visibility yet.
- Do not implement exclusion/degradation transitions.

### Inputs
- T24 normalized candidate contract
- Milestone 1 domain model foundations
- `design_phase/product_spec.md` FR-9a/FR-9b

### Relevant Context
M2 requires candidate listing records and stable identifiers before ranking and change tracking can be auditable.

### Implementation Budget
- Production files: max 3-5 candidate-storage focused files.
- Behavior count: 1 behavior (idempotent candidate + identifier persistence).
- Model/API/workflow count: up to 2 tightly-coupled model families (candidate listings + identifiers), 0 APIs, 1 worker write flow.
- Test scope: persistence idempotency and identifier continuity updates.

### Expected Output
Discovery workflow can store candidate listings and associated identifiers without creating duplicates.

### Acceptance Criteria
- Candidate listings are persisted per discovered entity/listing.
- Stable identifiers are stored when provided.
- Repeated ingest of same candidate updates safely without duplicate identity drift.

### Test Requirements
- Idempotent upsert tests for candidate listings.
- Identifier capture/update tests.

### Dependencies
- T24.

### Stop Condition
Stop when candidate and identifier persistence is reliable and tested.

### Completion Format
Implementation agent must finish task file with:
- `### Status`
- `Done.`
- `### Completion Summary`
- concise summary of completed work

### Status
Done.

### Completion Summary
- Added worker-facing discovery candidate persistence service that idempotently upserts `Instrument` and `Listing` records from normalized discovery candidates.
- Added stable identifier persistence for discovery candidates with provider-scoped identifier upsert behavior to preserve continuity on repeated ingests.
- Added focused persistence tests covering listing/idempotent ingest behavior and identifier capture/update behavior.

