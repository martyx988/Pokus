## T24: Instrument Discovery Adapter Interface and Normalized Candidate Contract

### Purpose
Define provider-agnostic discovery adapter boundary and normalized candidate schema for universe discovery.

### Scope
Implement interface/contracts only, plus stub/fake adapter for tests. No production provider integration yet.

### Allowed Changes
- Discovery adapter interface modules
- Candidate DTO/schema/validator modules
- Contract tests with fake adapter

### Forbidden Changes
- Do not call live external providers.
- Do not perform persistence, ranking, or support-state mutations.
- Do not implement price ingestion.

### Inputs
- `design_phase/architecture.md` universe management and adapter boundary
- `design_phase/product_spec.md` FR-4, FR-9a, FR-9b needs
- T23 calendar resolver availability

### Relevant Context
Universe discovery must be provider-isolated. Downstream tasks require stable normalized candidate fields including identifiers when available.

### Implementation Budget
- Production files: max 2-4 adapter-contract files.
- Behavior count: 1 behavior (normalized discovery candidate contract).
- Model/API/workflow count: 0 DB models, 0 APIs, 1 adapter interface.
- Test scope: contract validation for required candidate fields and optional stable identifiers.

### Expected Output
A clear discovery adapter interface and validated normalized candidate payload contract exist for worker workflows.

### Acceptance Criteria
- Interface defines required discovery call shape and outputs.
- Candidate contract includes exchange, instrument type, symbol/name, and optional stable identifiers.
- Contract tests enforce normalization invariants.

### Test Requirements
- Adapter contract tests with fake adapter outputs (valid/invalid payloads).

### Dependencies
- T23.

### Stop Condition
Stop when adapter boundary and normalized candidate contract are stable and tested.

### Completion Format
Implementation agent must finish task file with:
- `### Status`
- `Done.`
- `### Completion Summary`
- concise summary of completed work

### Status
Done.

### Completion Summary
- Added a provider-agnostic discovery adapter boundary with a normalized request/response contract for universe discovery candidates.
- Added normalized candidate validation for required fields (`exchange`, `instrument_type`, `symbol`, `name`) and optional `stable_identifiers`.
- Added contract tests using a fake adapter to verify valid payload normalization and invalid payload rejection.

