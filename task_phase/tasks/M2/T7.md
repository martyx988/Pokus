## T7: Exchange Activity Priority Computation and Monthly Recompute Path

### Purpose
Compute exchange activity priority from trailing 60 trading-day traded value (normalized) and provide monthly recompute entrypoint.

### Scope
Implement computation routine and scheduled/manual recompute path used by ranking tie-break logic.

### Allowed Changes
- Exchange priority computation service
- Scheduled/manual worker command entrypoint for monthly recompute
- Priority persistence fields and focused tests

### Forbidden Changes
- Do not change listing ranking precedence rules.
- Do not implement full provider-validation reporting.
- Do not build operator dashboards in this task.

### Inputs
- `design_phase/product_spec.md` FR-6a and model relation constraints
- T3 calendar expected-day resolver
- T6 ranking service consumer requirements

### Relevant Context
Activity priority is part of global listing policy and must recompute during validation/discovery and monthly thereafter.

### Implementation Budget
- Production files: max 2-4 priority-focused files.
- Behavior count: 1 behavior (priority recompute and persistence).
- Model/API/workflow count: 1 exchange-priority model/update path, 0 APIs, 1 scheduled/admin workflow.
- Test scope: normalized computation correctness and safe recompute update behavior.

### Expected Output
System can compute and persist exchange activity priorities and rerun computation on monthly cadence path.

### Acceptance Criteria
- Priority values derive from trailing 60 expected trading-day turnover with normalization strategy.
- Recompute path can be invoked repeatedly without inconsistent duplicates.
- Ranking service can consume updated priority values.

### Test Requirements
- Unit tests for computation math/ordering.
- Workflow test for monthly recompute invocation and persistence update.

### Dependencies
- T3
- T6

### Stop Condition
Stop when priority computation and recompute path are stable and consumable by ranking.

### Completion Format
Implementation agent must finish task file with:
- `### Status`
- `Done.`
- `### Completion Summary`
- concise summary of completed work
