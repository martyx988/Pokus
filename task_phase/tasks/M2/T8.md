## T8: Supported Universe State Projection from Ranked Listings

### Purpose
Project ranked listing decisions into final supported-universe state used by downstream app and worker flows.

### Scope
Implement support-state write/update flow that marks exactly chosen listings as supported and keeps unsupported candidates non-visible.

### Allowed Changes
- Supported-universe state service/module
- Persistence updates for chosen-listing support markers
- Focused tests for one-supported-listing-per-entity behavior

### Forbidden Changes
- Do not implement exclusion/degradation event transitions here.
- Do not add app-facing endpoint logic.
- Do not implement publication/readiness logic.

### Inputs
- Ranking output from T6
- Priority values from T7
- Candidate records from T5

### Relevant Context
M2 validation requires eligible supported universe creation per configured launch exchanges, and app must not see unsupported candidates.

### Implementation Budget
- Production files: max 2-4 support-state files.
- Behavior count: 1 behavior (materialize supported listing set).
- Model/API/workflow count: 1 support-state model/path, 0 APIs, 1 worker projection workflow.
- Test scope: one-final-listing invariants and scope-limited visibility state.

### Expected Output
Final supported universe state exists per configured scope with exactly one chosen listing per multi-listed entity.

### Acceptance Criteria
- Supported state is created only for configured exchanges and instrument types.
- Exactly one listing per entity is marked supported after ranking.
- Unsupported candidates remain non-supported.

### Test Requirements
- Integration test for projection from candidate set to supported universe state.
- Invariant test enforcing single supported listing per entity.

### Dependencies
- T6
- T7

### Stop Condition
Stop when supported-universe projection is deterministic and scoped.

### Completion Format
Implementation agent must finish task file with:
- `### Status`
- `Done.`
- `### Completion Summary`
- concise summary of completed work
