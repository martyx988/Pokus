## T30: App-Facing Supported Universe Read Endpoint

### Purpose
Expose app-facing read surface that returns only supported instruments and support/signal-readiness state.

### Scope
Implement or finalize public read endpoint/query path for supported universe output, excluding unsupported candidates.

### Allowed Changes
- App/public API read handler modules
- Read-model/query modules required for supported universe output
- Focused API tests for visibility and response contract

### Forbidden Changes
- Do not add private operator/admin endpoints beyond existing scope.
- Do not add current-day price publication behavior.
- Do not include unsupported candidate listings in response.

### Inputs
- Supported-universe state from T28
- Universe-change and identity continuity context from T29
- `design_phase/product_spec.md` FR-8 and Milestone 2 acceptance criteria

### Relevant Context
Milestone 2 completes when app can retrieve supported universe only, with readiness/support state and no unsupported exposure.

### Implementation Budget
- Production files: max 2-4 read-endpoint files.
- Behavior count: 1 behavior (supported-universe public retrieval).
- Model/API/workflow count: 0 new models, 1 public API surface, 0 publication workflows.
- Test scope: API visibility boundaries and response-shape correctness.

### Expected Output
Public API endpoint returns only final supported universe entries for configured scope.

### Acceptance Criteria
- Endpoint returns supported instruments only.
- Response includes support/signal-readiness state fields required by M2 scope.
- Unsupported candidates are never returned.

### Test Requirements
- API test proving supported-only filtering.
- API test proving unauthorized private fields are absent.

### Dependencies
- T28
- T29

### Stop Condition
Stop when app-facing supported-universe retrieval is correct and visibility-safe.

### Completion Format
Implementation agent must finish task file with:
- `### Status`
- `Done.`
- `### Completion Summary`
- concise summary of completed work

